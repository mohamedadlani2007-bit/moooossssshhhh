# تغييرات المسار والواجهة

تم تعديل **النسخة المعدّلة فقط**، بينما بقيت نسخة الصورة الأصلية محفوظة دون تغيير.

| العنصر | قبل | بعد |
|---|---|---|
| مسار WebSocket | `/by_old_ssh` | `/_mohalamia` |
| الصفحة الرئيسية بعد إعداد البوت | JSON يعرض معلومات الخدمة والدومين والمسار | صفحة تعرض: **تم الاتصال بنجاح** |
| معلومات الدخول في الصفحة الرئيسية | كانت ظاهرة ضمن رد الحالة | أزيلت من الواجهة الرئيسية |

صفحة إعداد البوت تبقى متاحة عندما لا يكون البوت مضبوطًا بعد. تم فحص صحة السكريبت المجمّع نحويًا، ولم يتم تشغيل الحاوية أو فتح أي اتصال شبكي بها.

يجب أن يستخدم عميل الاتصال المسار الجديد `/_mohalamia`، وإلا فلن يطابقه الخادم.
